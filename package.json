{
  "name": "skillmap-ai-root",
  "version": "1.0.0",
  "private": true,
  "description": "SkillMap AI - AI Career Intelligence & Verification Platform",
  "scripts": {
    "postinstall": "npm --prefix frontend install && npm --prefix frontend run build && node -e \"require('fs').cpSync('frontend/dist', 'dist', {recursive: true})\"",
    "build": "npm --prefix frontend install && npm --prefix frontend run build && node -e \"require('fs').cpSync('frontend/dist', 'dist', {recursive: true})\"",
    "start": "node serve-app.js",
    "dev": "npm --prefix frontend run dev"
  }
}
